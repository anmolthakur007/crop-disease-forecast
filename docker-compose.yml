version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: cropsentinel-db
    environment:
      POSTGRES_DB: cropsentinel
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports: 
      - "5432:5432"